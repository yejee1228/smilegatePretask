import React from 'react';
import CreateCode from '@/components/user/CreateCode';

const Index = () => {
    return (
        <>
            <CreateCode />
        </>
    );
};

export default Index;